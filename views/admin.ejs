<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Portal</title>
    <style>
        * {box-sizing: border-box;}
        body {
            background-image: linear-gradient(blue, rgb(64, 64, 255), rgb(115, 115, 183));
            background-repeat: no-repeat;
            width: 100vw;
            height: 100vh;
            margin: 0px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: sans-serif;
        }

        .loginDiv {
            width: 500px;
            padding: 20px;
            background-color: rgba(0, 0, 0, 0.7);
            border-radius: 10px;
        }

        input {
            border: none;
            border-radius: 50px;
            outline: none;
            font-size: 1em;
            width: 100%;
            padding: 12px;
            margin-bottom: 14px;
        }

        h1 {
            color: white;
            font-size: 1.4em;
            margin: 10px 0px;
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            text-align: center;
        }

        button {
            border: 2px solid dodgerblue;
            color: dodgerblue;
            font-size: 1.2em;
            padding: 12px;
            cursor: pointer;
            background-color: transparent;
            border-radius: 50px;
            transition: 0.3s;
        }

        button:hover {
            background-color: dodgerblue;
            color: black;
        }
    </style>
</head>
<body>
    <div class="loginDiv">
        <form action="" method="POST">
            <h1>Login To The Admin Portal</h1>
            <input type="text" placeholder="Username:" name="username" autocomplete="off" required id="username">
            <input type="password" placeholder="Password:" name="password" required id="password">
            <button type="submit" onclick="handleSubmit(event)">Login!</button>
        </form>
    </div>
    <script>
        function handleSubmit(e) {
            e.preventDefault()
            if (document.getElementById('username').value === 'adminUser1011' && document.getElementById('password').value === 'RD7fDi6rD$a') {
                window.location.href = '/admin/redirect/verify/RD7fDi6rDaRD7fDi6rDa'
            }
        }
    </script>
</body>
</html>